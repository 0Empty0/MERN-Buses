import Modal from '@/components/Layout/Modal/Modal'
import { FC } from 'react'

import styles from './LoadingSpinner.module.scss'

const LoadingSpinner: FC = () => {
	return <Modal>LoadingSpinner</Modal>
}

export default LoadingSpinner
